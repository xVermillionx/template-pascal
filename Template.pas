PROGRAM Template;

PROCEDURE proc(s:STRING; VAR i:INTEGER);
VAR
  sLen, pLen: INTEGER;
  i, j: INTEGER;
BEGIN
  WriteLn(main, i);
END;


VAR
  main: STRING;
  i: INTEGER;
BEGIN

  proc(main, i);

END;
